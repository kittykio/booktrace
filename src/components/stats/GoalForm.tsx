'use client'; import { useActionState } from 'react'; import { saveGoal } from '@/lib/actions';
export default function GoalForm({ year, target }: { year: number; target: number }) { const [state, action, pending] = useActionState(saveGoal, {}); return <form action={action} className="card flex flex-wrap items-end gap-4"><input type="hidden" name="year" value={year} /><label>{year} reading goal<input className="w-32" name="target" type="number" min="1" max="1000" defaultValue={target} /></label><button className="button" disabled={pending}>{pending ? 'Saving…' : 'Update goal'}</button><p role="status">{state.error ?? state.success}</p></form>; }
